const GeneratedWords = ({ words }: { words: string }) => {
  return (
    <div
      // initial={{ opacity: 0 }}
      className="text-slate-600"
    >
      {words}
    </div>
  );
};

export default GeneratedWords;
